# Copy to .env and fill in. Never commit the real .env — it's gitignored.
# The var name here must match config.json -> telegram.botTokenEnv.
HERMES_CLAUDE_BRIDGE_BOT_TOKEN=123456:your-telegram-bot-token
